/**
 * Accessible Components - Stub implementations
 */

export const AccessibleSkipLink = ({ href, children }: any) => null;
export const AccessibleBreadcrumb = ({ items }: any) => null;
export const AccessibleLoading = ({ message }: any) => null;
export const AccessibleButton = ({ onClick, children }: any) => null;
export const AccessibleAlert = ({ type, message }: any) => null;
