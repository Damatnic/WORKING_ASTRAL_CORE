/**
 * Auth utilities - Stub implementations
 */

export function hasRole(user: any, role: string): boolean {
  return false;
}

export function hasPermission(user: any, permission: string): boolean {
  return false;
}
